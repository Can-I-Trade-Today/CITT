[build]
  base = "apps/cron/"
  command = "pnpm install && pnpm build"
  functions = "functions"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF ."
[functions."cron-job"]
  schedule = "@hourly"